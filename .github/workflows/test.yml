name: Build and test

on:
  push:

jobs:
  test:
    name: Build
    runs-on: 'ubuntu-latest'
    steps:
      - uses: actions/checkout@v4

      - name: Setup go
        uses: actions/setup-go@v4
        with:
          go-version: '1.21.x'
      - run: go version
